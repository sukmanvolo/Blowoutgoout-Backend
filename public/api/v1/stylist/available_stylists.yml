get:
  description: Get availables stylists (No functional)
  tags:
    - Stylist
  parameters:
    - name: Authorization
      in: header
      description: "JWT token"
      type: string
    - name: params
      in: body
      description: Stylist data
      schema:
        $ref: '#/components/schemas/availability_params'
components:
  schemas:
    availability_params:
      type: object
      properties:
        lat:
          type: string
        long:
          type: string
        service_ids:
          type: array
        date:
          type: string
        start_time:
          type: string